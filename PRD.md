# PRD: 직장 헬게이트 탈출 v3.0

**Product Requirements Document**
버전: 3.0 | 작성일: 2026-03-11 | 이전 버전: v2.0

---

## 1. 제품 개요

### 1.1 제품명
직장 헬게이트 탈출 (Workplace Hellgate Escape)

### 1.2 제품 비전
직장인이 일상에서 겪는 스트레스를 게임을 통해 재미있고 안전하게 해소할 수 있는 모바일 웹 탭 액션 게임

### 1.3 핵심 가치 제안
- 실제 직장 내 스트레스 유발자(빌런)를 직접 등록하고 두들겨 패며 카타르시스 제공
- 출퇴근, 점심시간 등 짧은 시간(5~10분)에 부담 없이 즐길 수 있는 캐주얼 경험
- 별도 설치 없이 모바일 웹 브라우저에서 즉시 플레이 가능

### 1.4 타겟 사용자
- **주요 타겟:** 직장인 (20~40대)
- **사용 맥락:** 출퇴근 대중교통, 점심시간 휴식, 야근 중 잠깐의 스트레스 해소

### 1.5 플랫폼 및 배포
- **플랫폼:** 모바일 웹 (PWA 형태)
- **배포:** Vercel (https://hell-escape.vercel.app)
- **기기 우선순위:** 모바일 스마트폰 (태블릿, 데스크톱 호환)

---

## 2. 성공 지표 (Success Metrics)

| 지표 | 목표 |
|------|------|
| 세션 평균 플레이 시간 | 5~10분 |
| 재방문율 (7일 리텐션) | 30% 이상 |
| 리더보드 등록율 | 전체 플레이어의 50% 이상 |
| 무기 다양성 사용율 | 4종 무기 모두 1회 이상 사용 비율 40% 이상 |

---

## 3. 사용자 흐름 (User Flow)

```
[설정 화면 (Setup)]
       ↓
  빌런 등록 & 닉네임 & 무기 선택
       ↓
[게임 화면 (Battle)]
       ↓
  탭 전투 → 빌런 피해 누적 시각화 → 빌런 격파
       ↓
[선택 이벤트 (Choice Modal)]  ← 마지막 빌런이 아닐 경우
       ↓
  다음 빌런 등장 → 전체 격파
       ↓
[결과 화면 (Result)]
       ↓
  점수 확인 → 리더보드 등록 → 다시 도전
```

---

## 4. 기능 요구사항

### 4.1 설정 화면 (Setup Screen)

#### 4.1.1 플레이어 닉네임
- 리더보드 등록에 사용되는 닉네임 입력 필드
- 필수 입력값 (비어있을 경우 게임 시작 불가)

#### 4.1.2 빌런 등록
- **빌런 유형 선택 (5종 프리셋):** 꼰대 상사 / 뒤통수 동료 / 무한 잔소리 / 야근 강요러 / 책임 전가러
- **빌런 이름 입력:** 직접 커스텀 (예: 김부장, 박과장)
- **빌런 특징 입력:** 메모 형식의 특징 입력
- **다중 빌런 등록 / 개별 삭제** 지원
- 최소 1명 이상의 빌런이 등록되어야 게임 시작 가능

#### 4.1.3 무기 선택 (신규 — FB-003)
게임 시작 전 무기 1종을 선택. 선택한 무기는 게임 전체에 적용.

| 무기 | 아이콘 | 공격 특성 | 타격 이펙트 |
|------|--------|-----------|------------|
| 망치 | 🔨 | 중타격, 느린 콤보 | 멍·골절 시각화 |
| 칼 | 🔪 | 빠른 다단 타격 | 출혈·살점 시각화 |
| 총 | 🔫 | 원거리, 구멍 누적 | 탄흔 누적 시각화 |
| 불 | 🔥 | 지속 피해, 광역 | 불꽃 번짐 시각화 |

- 무기 선택 UI: 설정 화면 하단 4-grid 버튼
- 선택하지 않으면 기본 무기(주먹 👊) 적용

#### 4.1.4 리더보드 접근
- 설정 화면에서 현재 상위 10개 기록(명예의 전당) 확인 가능

---

### 4.2 게임 화면 (Battle Screen)

#### 4.2.1 상단 HUD

| 게이지 | 설명 | 동작 |
|--------|------|------|
| ❤️ HP 게이지 | 현재 빌런의 체력 | 공격 시 감소, 0이 되면 격파 |
| 😤 스트레스 게이지 | 플레이어의 스트레스 수치 | 공격할수록 감소 |
| ⚡ 필살기 게이지 | 필살기 충전량 | 공격/콤보 시 충전, 100%에서 사용 가능 |

#### 4.2.2 전투 시스템

| 기능 | 상세 사양 |
|------|----------|
| **탭 공격** | 빌런 탭/클릭 시 무기별 데미지 범위 적용 |
| **콤보 시스템** | 연속 탭 시 콤보 카운트 증가 → 데미지 배율 상승 |
| **콤보 초기화** | 마지막 탭 후 1.6초 경과 시 콤보 카운트 리셋 |
| **필살기** | 게이지 100% 충전 후 필살기 버튼 탭 → 대형 피해 + 화려한 이펙트 |
| **빌런 감정 변화** | HP 잔량에 따라 이모지 변화: 😤 → 😰 → 😱 → 🤮 → 💀 |
| **다중 빌런 순환** | 현재 빌런 격파 시 선택 이벤트 → 다음 빌런 등장 |

**무기별 데미지 범위:**

| 무기 | 기본 데미지 | 콤보 보너스 | 특수 효과 |
|------|------------|------------|-----------|
| 주먹 👊 | 4~11 | +1.5×콤보 | 없음 (기존 동작 유지) |
| 망치 🔨 | 8~18 | +2×콤보 | 콤보 5↑시 기절(0.5초 추가 데미지) |
| 칼 🔪 | 3~7 (×3회) | +1×콤보 | 출혈: 1초마다 2 추가 데미지 (3초) |
| 총 🔫 | 10~20 | +0.5×콤보 | 탄흔 누적 (시각 효과) |
| 불 🔥 | 5~10 | +3×콤보 | 점화: 2초마다 5 지속 피해 (5초) |

#### 4.2.3 피해 누적 시각화 (신규 — FB-001, FB-002)

빌런 이모지 주변에 피해 레이어를 CSS 오버레이로 표현. HP 감소량에 따라 단계적으로 추가됨.

**무기별 피해 오버레이:**

| 무기 | 25% HP 손실 | 50% HP 손실 | 75% HP 손실 | 90%+ HP 손실 |
|------|------------|------------|------------|--------------|
| 망치 🔨 | 멍 이모지 💜×1 | 💜×3 + 🩹 | 골절 표시 💥 | 전신 멍 + 흔들림 강화 |
| 칼 🔪 | 상처선 (CSS border) | 빨간 점 🔴×2 | 붉은 오버레이 | 피투성이 플래시 |
| 총 🔫 | 탄흔 ⚫×1 | ⚫×3 | ⚫×6 (너덜너덜) | ⚫×10 + 생선가시 CSS |
| 불 🔥 | 불꽃 테두리 | 주황 글로우 강화 | 검은 그을림 | 재(ash) CSS 필터 |

- 오버레이는 빌런 이모지를 감싸는 `position: relative` 컨테이너에 `::after` 또는 절대 위치 자식 요소로 구현
- HP 단계가 바뀔 때마다 `damageStage` state(0~4) 업데이트 → 해당 단계의 클래스 추가

#### 4.2.4 갱생 기회 이벤트 (신규 — FB-008)

빌런 HP가 **최초로 30% 이하**로 떨어지는 순간 1회 한정으로 "구해주시겠습니까?" 모달 표시.
격파 후 선택 이벤트(4.2.5)와 별개로, **전투 도중** 발동하는 자비 판단 이벤트.

**모달 구성:**
- 빌런 상태: 눈물 이모지(😭)로 변경 + 애원 메시지 표시
- 선택지 2종:

| 선택지 | 아이콘 | 효과 |
|--------|--------|------|
| 갱생시킨다 | 🕊️ | 전투 종료 + 해당 빌런 격파 처리 + 갱생 보너스 **+300점** |
| 계속 공격한다 | ⚡ | 모달 닫고 전투 계속 (보너스 없음) |

- 5초 타임아웃 → 미선택 시 "계속 공격" 자동 선택
- 갱생 선택 시: 빌런이 😭→🙏 이모지로 전환 후 퇴장 애니메이션, 다음 빌런으로 진행
- HP 30% 이하 재진입해도 **1빌런당 1회만** 표시
- 결과 화면 선택 이력에 갱생 선택 여부 표시

#### 4.2.5 선택 이벤트 (신규 — FB-005)

빌런 격파 후 다음 빌런 등장 직전(마지막 빌런 제외) 도덕적 딜레마 모달 표시.

**모달 구성:**
- 격파한 빌런 정보 표시
- 행동 선택 3종:

| 선택지 | 아이콘 | 점수 효과 | 다음 빌런 효과 |
|--------|--------|-----------|--------------|
| 마무리 | ⚡ "이대로 끝낸다" | +100점 | 없음 |
| 구호품 전달 | 📦 "구호품을 보낸다" | -50점 | 다음 빌런 HP -20 (약해짐) |
| 치료 지원 | 💊 "치료를 지원한다" | +200점 | 다음 빌런 HP +30 (강해짐) |

- 선택 후 3초 내 자동 진행 (타이머 표시)
- 선택하지 않으면 "마무리" 자동 선택
- 결과 화면에 선택 이력 요약 표시

#### 4.2.5 시각 이펙트 (기존 + 확장)

| 이펙트 | 사양 |
|--------|------|
| 플로팅 텍스트 | 무기별 타격 문구 + 콤보 수 표시 |
| 충격파 애니메이션 | 무기별 색상·크기 변형 (망치=노랑, 칼=빨강, 총=흰색, 불=주황) |
| 화면 흔들림 | 무기별 진폭 차등 (망치 > 주먹 > 칼 > 불 > 총) |
| 배경 플래시 | 무기별 플래시 색상 변경 |
| 피해 누적 오버레이 | HP 단계별 시각 피해 표현 (신규) |
| **무기 타격 애니메이션** | **탭 시 무기 이모지가 화면 아래(1인칭 시점)에서 빌런을 향해 솟아오르는 CSS 키프레임 애니메이션 (신규 — FB-007)** |

#### 4.2.6 무기 타격 애니메이션 상세 (신규 — FB-007)

탭/클릭 시 선택한 무기가 화면 하단에서 빌런을 가격하는 1인칭 시점 타격 연출.
플로팅 텍스트·충격파와 함께 동시 발생하여 타격감 극대화.

| 무기 | 애니메이션 연출 |
|------|--------------|
| 주먹 👊 | 주먹 이모지가 하단에서 위로 솟구쳐 빌런에 닿는 순간 확대 후 사라짐 |
| 망치 🔨 | 망치가 위에서 아래로 내리치는 궤적 (arc) 애니메이션 |
| 칼 🔪 | 칼날이 대각선으로 빠르게 가로지르는 슬래시 애니메이션 |
| 총 🔫 | 총구 화염(💥) + 총이 약간 반동(kickback)하는 애니메이션 |
| 불 🔥 | 불덩어리가 빌런을 향해 날아가 퍼지는 애니메이션 |

- 구현 방식: 무기 이모지를 `position: fixed` 절대 좌표로 배치, `@keyframes` CSS로 이동·스케일·opacity 제어
- 지속 시간: 250~350ms (타격 반응 지연 최소화)
- 탭 좌표 기반으로 무기 이동 방향 동적 계산

**무기별 타격 문구 확장:**

| 무기 | 타격 문구 |
|------|----------|
| 주먹 | 퍽!! / 쾅!! / 받아랏! / BOOM!! |
| 망치 🔨 | 쾅!!! / 박살! / 뼈가! / 으드득! / 작살! |
| 칼 🔪 | 스읏! / 찌릿! / 베었다! / 피다! / 살점! |
| 총 🔫 | 빵야! / 탕!! / 구멍! / 저격! / 헤드샷! |
| 불 🔥 | 활활! / 타올라! / 불이야! / 재가돼라! / 뜨겁지?! |

---

### 4.3 결과 화면 (Result Screen)

#### 4.3.1 표시 통계

| 항목 | 계산 방식 |
|------|----------|
| 총 점수 | 공격 횟수×10 + 최대 콤보×50 + 격파 빌런 수×200 + 스트레스 해소율×5 + 선택 이벤트 보너스 합산 |
| 총 공격 횟수 | 플레이 중 전체 탭 횟수 합산 |
| 최대 콤보 | 게임 중 기록한 최고 콤보 수 |
| 격파 빌런 수 | 처치한 빌런 총 인원 |
| 스트레스 해소율 | 게임 중 감소한 스트레스 수치의 % |
| 선택 이벤트 이력 | 각 빌런에서 선택한 행동 요약 (신규) |

#### 4.3.2 리더보드 등록
- 게임 종료 후 닉네임과 점수를 로컬 리더보드에 자동 저장
- 상위 10개 기록만 유지

#### 4.3.3 액션 버튼
- **다시 도전:** 설정 화면으로 돌아가기

---

### 4.4 사운드 시스템 (확장)

| 사운드 | 구현 방식 | 상세 |
|--------|----------|------|
| 배경음악 | Web Audio API 비트 루프 | 기존 유지 |
| 타격음 (주먹) | Web Audio API | 기존 유지 |
| 타격음 (망치) | Web Audio API | 저주파 충격음, 골진동 느낌 |
| 타격음 (칼) | Web Audio API | 고주파 슬라이딩음 |
| 타격음 (총) | Web Audio API | 임펄스 노이즈 |
| 타격음 (불) | Web Audio API | 치직거리는 노이즈 루프 |
| 필살기 | Web Audio API | 기존 유지 |
| 격파음 | Web Audio API | 기존 유지 |
| 선택 이벤트 | Web Audio API | 결정 효과음 (신규) |

---

### 4.5 진동 피드백 (확장)

| 상황 | 진동 패턴 |
|------|----------|
| 주먹 타격 | [20] |
| 망치 타격 | [50, 20, 30] (묵직한 패턴) |
| 칼 타격 | [10, 10, 10] (빠른 3연속) |
| 총 타격 | [15] (짧고 강하게) |
| 불 타격 | [30, 10, 20, 10] (지글지글) |
| 콤보 5↑ 타격 | [30, 10, 30] |
| 필살기 | [100, 50, 100, 50, 200] |
| 빌런 격파 | [50, 30, 50, 30, 100] |

---

### 4.6 리더보드

| 항목 | 사양 |
|------|------|
| 저장 방식 | `localStorage` (기기 내 로컬 저장) |
| 저장 용량 | 상위 10개 기록만 유지 |
| 표시 항목 | 순위 / 닉네임 / 점수 / 사용 무기 / 날짜 |
| 접근 시점 | 설정 화면 및 결과 화면 양쪽에서 확인 가능 |

---

## 5. 비기능 요구사항

### 5.1 성능
- 첫 로드 후 게임 시작까지 2초 이내
- 탭 입력 반응 지연 100ms 이하
- 피해 오버레이 추가로 인한 렌더링 지연 없음 (CSS transition 활용)

### 5.2 접근성 및 UX
- 한 손 세로 모드 조작 최적화
- 음소거 기능 기본 제공
- 진동 미지원 기기에서도 정상 동작
- 선택 이벤트 모달: 타이머 3초 + 자동 진행으로 답답함 방지

### 5.3 브라우저 호환성
- iOS Safari, Android Chrome 최신 2개 메이저 버전 지원
- Web Audio API, Vibration API, localStorage 사용 (미지원 시 graceful degradation)

---

## 6. 기술 스택

| 항목 | 기술 |
|------|------|
| 언어 / UI | React (JSX), CSS-in-JS (인라인 style 태그) |
| 사운드 | Web Audio API (프로그래매틱 생성) |
| 진동 | Vibration API |
| 데이터 저장 | localStorage |
| 빌드 | Vite |
| 배포 | Vercel (GitHub 연동, 자동 배포) |

---

## 7. 개발 로드맵 (v3.0 기준)

### Phase 1 — 무기 시스템 기반 구축
> 가장 핵심 변경. 이후 Phase의 기반이 됨.

- [v] `WEAPONS` 상수 정의 (id, emoji, label, damage, hitWords, vibration)
- [v] 설정 화면에 무기 선택 UI 추가 (4-grid)
- [v] `selectedWeapon` state 추가, `startGame()`에 전달
- [v] `handleHit`에 무기별 데미지·사운드·진동·타격 문구 분기
- [v] `AudioEngine`에 무기별 타격음 메서드 추가 (`playHitWeapon(weapon, combo)`)
- [v] 충격파·배경 플래시 색상 무기별 분기

### Phase 2 — 피해 누적 시각화
> Phase 1 완료 후 진행.

- [v] `damageStage` state 추가 (0~4, HP 구간별 자동 갱신)
- [v] 빌런 표시 영역을 `position: relative` 컨테이너로 감싸기
- [v] 무기별 × 단계별 오버레이 CSS 클래스 정의 (망치/칼/총/불 각 4단계)
- [v] `loadVillain()` 호출 시 `damageStage` 초기화
- [v] 총 무기: 탄흔(⚫) 누적 렌더링 (배열 state)

### Phase 3 — 선택 이벤트 시스템
> Phase 1 완료 후 진행 (Phase 2와 병렬 가능).

- [v] `choiceEvent` state 추가 (null | { active, villain })
- [v] `triggerDefeat()`에서 마지막 빌런이 아닌 경우 모달 표시
- [v] 선택지 3종 UI 구현 (모달 컴포넌트)
- [v] 3초 자동 진행 타이머 구현
- [v] 선택 결과 누적 (`choiceHistory` state), 결과 화면에 표시
- [v] 점수 계산식 업데이트 (선택 보너스 반영)

### Phase 4 — 점수 밸런싱 & 리더보드 업데이트
> Phase 1~3 완료 후 진행.

- [v] 무기별 점수 배율 조정 (fist×1.0 / hammer×1.1 / knife×1.2 / gun×1.1 / fire×1.15)
- [v] 선택 이벤트 보너스/패널티 수치 밸런싱 (finish+100 / supply-50 / heal+200)
- [v] 리더보드 저장 항목에 `weapon` 추가
- [v] `localStorage` 키 `hellgate_leaderboard_v3`으로 마이그레이션

### Phase 5 — 타격 몰입감 강화 (신규 — FB-007, FB-008)
> Phase 1~2 완료 후 진행 가능.

**무기 타격 애니메이션 (FB-007)**
- [ ] `WeaponStrike` 컴포넌트 구현 — 탭 시 무기 이모지가 1인칭 시점으로 등장
- [ ] 무기별 궤적 애니메이션 CSS `@keyframes` 정의 (솟아오름/내려침/슬래시/반동/투사체)
- [ ] `handleHit`에서 탭 좌표 기반으로 `WeaponStrike` 인스턴스 생성 후 자동 제거
- [ ] 250~350ms 지속 시간으로 타격감 유지하면서 렌더링 부담 최소화

**갱생 기회 이벤트 (FB-008)**
- [ ] `redemptionShown` state 추가 (빌런별 1회 표시 추적)
- [ ] `handleHit` 내 HP 30% 이하 진입 감지 → `redemptionEvent` state 세팅
- [ ] 갱생 모달 UI 구현 (5초 타임아웃, 선택지 2종)
- [ ] 갱생 선택 시 빌런 퇴장 애니메이션(😭→🙏) + +300점 보너스 처리
- [ ] `loadVillain()` 호출 시 `redemptionShown` 초기화
- [ ] 결과 화면 선택 이력에 갱생 이벤트 포함

---

## 8. 범위 외 (Out of Scope, v3.0 기준)

- 서버 사이드 기능 (백엔드, DB, 인증)
- 글로벌 온라인 리더보드
- 소셜 로그인 / 인앱 결제 / 광고
- 빌런 커스텀 이미지 업로드
- 선택 이벤트의 멀티 엔딩 분기 (v4.0 검토)

---

## 9. 오픈 이슈 및 결정 사항

| 이슈 | 상태 | 비고 |
|------|------|------|
| 무기별 데미지 밸런스 수치 확정 | 미결 | 플레이테스트 후 조정 |
| 선택 이벤트 자동 진행 시간 (3초 적절성) | 미결 | UX 테스트 필요 |
| 피해 오버레이 렌더링 성능 (저사양 기기) | 미결 | CSS only로 제한 |
| 총 무기 탄흔 최대 개수 | 미결 | 10개로 제한 고려 |
| 불 무기 지속 피해 타이머 정리 시점 | 미결 | 빌런 전환 시 clearInterval 필요 |
| 무기 타격 애니메이션 동시 다중 표시 제한 | 미결 | 최대 1~2개 동시 표시로 제한 검토 |
| 갱생 이벤트 타임아웃 시간 (5초 적절성) | 미결 | 선택 이벤트 3초와 통일 여부 검토 |
| 갱생 후 점수 밸런싱 (+300점 과다 여부) | 미결 | 플레이테스트 후 조정 |

---

## 10. 신규 피드백 (배포 후 수집)

### FB-006 · 무기별 액티브 피해 시각화 강화 (2026-03-11)

> "다양한 무기가 추가된 만큼 무기의 특성을 살린 효과가 필요함.
> 빌런이 공격 받을 때 조금 더 액티브했으면 좋겠어.
> 예) 총이 발사해 구멍이 난다거나 칼로 찢겨서 뼈만 남는다거나 화형 시 불타오른다거나"

| 무기 | 현재 구현 | 요청 |
|------|----------|------|
| 🔫 총 | ⚫ 탄흔 이모지 누적 (정적) | 타격마다 구멍 뚫리는 팝인 애니메이션 |
| 🔪 칼 | 🩸 피 이모지 누적 (정적) | 살점 → 뼈대 노출 단계 표현 |
| 🔥 불 | 🔥 이모지 단계 표시 (정적) | 불꽃이 번지는 연소 애니메이션 |
| 🔨 망치 | 💜 멍 이모지 표시 (정적) | 강타 시 형태 변형·흔들림 이펙트 |

**개발 방향**
- Phase 2 `DamageOverlay`를 정적 이모지에서 **CSS 키프레임 애니메이션** 기반으로 개선
- 타격 시 신규 오버레이 요소가 `pop-in` 애니메이션으로 등장하도록 트리거
- 무기별 누적 피해가 시각적으로 점층적으로 쌓이는 느낌 강화
- 모바일 성능을 고려해 CSS transform/opacity 기반 애니메이션으로 제한
- **1인칭 슈팅게임 참조**: 무기별 피해 표현이 FPS 게임처럼 피격 방식으로 차별화

---

### FB-007 · 무기 타격 애니메이션 (2026-03-12)
> "각 무기 선택 후 타격 시 해당 무기가 나와서 타격되는 모습이 나와야 함.
> 주먹이면 주먹이 나와서 타격, 총이면 총이 나와서 타격 — 액티브하게~"

| 무기 | 요청 연출 |
|------|----------|
| 👊 주먹 | 주먹 이모지가 화면 하단에서 솟구쳐 빌런을 가격 |
| 🔨 망치 | 망치가 위에서 아래로 호(arc)를 그리며 내리침 |
| 🔪 칼 | 칼날이 대각선으로 빠르게 슬래시 |
| 🔫 총 | 총구 화염 + 총기 반동(kickback) 연출 |
| 🔥 불 | 불덩어리가 빌런을 향해 날아가 확산 |

**개발 방향:** `WeaponStrike` 전용 컴포넌트 — 탭 좌표 기반 CSS 애니메이션, 300ms 지속 후 자동 제거

---

### FB-008 · HP 30% 갱생 기회 이벤트 (2026-03-12)
> "피 게이지 30% 정도 남으면 '구해주시겠습니까?' 메시지가 떠서
> 사용자에 따라 갱생의 기회를 줄 수 있으면 좋겠어."

| 항목 | 내용 |
|------|------|
| 발동 조건 | HP 최초 30% 이하 진입 시 (빌런당 1회) |
| 갱생 선택 | 전투 종료 + 격파 처리 + +300점 보너스 |
| 계속 공격 | 모달 닫고 전투 계속 |
| 타임아웃 | 5초 후 "계속 공격" 자동 선택 |

**개발 방향:** 격파 후 선택 이벤트와 별도로 `redemptionEvent` state로 관리. 빌런당 1회 제한.
